# Stage-2 backend deps — the FastAPI + Postgres game backend (app.main).
# The single requirements file for the backend image (Dockerfile.backend); the old
# websocket-server/requirements.txt was retired with main.py in S2.7.
SQLAlchemy[asyncio]==2.0.31
asyncpg==0.29.0
alembic==1.13.2

# S2.3 REST API + serving
fastapi==0.111.0
uvicorn==0.29.0          # ASGI server (serves app.main:app)
passlib[argon2]==1.7.4   # password hashing (argon2)

# S2.4 auth
PyJWT==2.8.0             # JWT access/refresh (HS256)
email-validator==2.1.1   # pydantic EmailStr

# test-only
pytest==8.4.2
pytest-asyncio==0.23.8
httpx==0.27.0            # FastAPI TestClient transport
