[pytest]
# game_engine.py uses flat imports (`from card_defs import ...`), so the
# websocket-server dir must be importable. `pythonpath = .` (rootdir) handles it.
pythonpath = .
testpaths = tests
addopts = -v --tb=short
